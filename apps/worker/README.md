# Worker component pointer

The implemented worker entry point is `python -m konsider.ingestion.worker`. Authoritative command,
source-version, output, validation, replay, rollback, and troubleshooting documentation is in
[worker operations](../../docs/operations/worker.md). Release contracts are described in
[release format](../../docs/data/release-format.md).
