# Release 2026-07-20.2

Status: published immutable local candidate; active release

Created and published: 2026-07-20

Schema: `konsider-release-3.0`

Release checksum: `sha256:d43d73d1919b5ce578911eb3cebcc6285514fcdc519503d57060ca5a21cdcecf`

## Result

The release contains 120 normalized observations, 120 scores, and 120 successful expected attempts:
six criteria for each of the fixed 20 countries. Structural validation passed with no errors. Five
criteria passed every product-readiness check, satisfying the configured aggregate publication gate.

| Criterion | Latest observation period | Readiness | Important interpretation |
| --- | --- | --- | --- |
| WDI population-weighted PM2.5 | 2023 | Ready | Modelled national exposure, not local monitoring. |
| WDI/UNODC intentional homicide | 2021-2023 | Ready | Reporting and legal definitions vary. |
| World Bank HNP UHC coverage | 2021 | **Not ready** | Complete coverage, but stale under the three-year freshness rule. |
| ICP household-consumption relative cost | 2025 | Ready | Broad discrete bands only; not a precise ranking or city budget. |
| WBL women’s legal and economic equality | data cutoff 2025-10-01 | Ready | De jure Legal Framework index, not enforcement or lived outcomes. |
| WDI infrastructure readiness experiment | components from 2022-2025 | Provisionally ready | Digital plus trade/transport scope; equal weights and mixed years. |

The validation report contains 20 UHC freshness blockers, one per expected country. These are scoped
to that criterion and are not hidden by the aggregate result. It also records ten non-blocking
material-change warnings relative to `2026-07-18.2`: eight PM2.5 changes following the source/method
transition and two ICP changes. Consumers must not interpret these source-transition differences as
time trends without review.

## Source and contract changes

- Replaced direct WHO PM2.5 with WDI `EN.ATM.PM25.MC.M3`.
- Replaced direct WHO UHC with World Bank HNP source 16 indicator `SH.UHC.SRVS.CV.XD`; the current
  WDI source-2 API returned no observations.
- Retained WDI `VC.IHR.PSRC.P5` with UNODC lineage.
- Retained ICP/WDI inputs but changed scoring to discrete broad relative-cost bands.
- Replaced GIWPS/PRIO WPS with the WBL 2026 Legal Framework economy index and renamed the criterion.
- Added an experimental equal-weight infrastructure composite from internet use, fixed broadband,
  and LPI infrastructure quality.
- Added per-component provenance, criterion-level readiness, schema version 3, source/scoring
  versions, payload/artifact/release checksums, timestamps, validation summary, and reproducibility
  metadata.

Raw source bytes remain content-addressed under ignored `data/raw/`. The committed release records
their exact retrieval metadata and checksums but does not redistribute them. Previous releases and
their files were not modified.

## Reproduction

With the locally retained raw artifacts:

```powershell
$env:PYTHONPATH = "src"
python -m konsider.ingestion.worker replay data\releases\2026-07-20.2
```

Replay verifies manifest file checksums and the aggregate release checksum, loads the embedded source
registrations, reparses exact artifacts, and compares regenerated observations and scores byte-for-byte
at the record-contract level.

Verification on 2026-07-20: 34 unit/integration tests passed, Python compilation passed, and replay of
both `2026-07-18.2` (schema v2) and `2026-07-20.2` (schema v3) passed using locally retained raw bytes.
