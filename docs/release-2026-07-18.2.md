# Release 2026-07-18.2

Status: published local stabilization release; structurally valid; product gate blocked

Published: 2026-07-18

This immutable successor reprocesses the unchanged raw inputs from experimental baseline
`2026-07-17.1`. It contains 100 record-level observations, 100 explicit successful source attempts,
100 threshold-based provisional scores, 20/20 coverage for five criteria, and 32 locally retained
raw artifacts referenced by checksum.

Structural validation passed with zero errors. Replay verifies every local raw checksum, every
release payload checksum, parsing, attempt generation, and scoring. Product readiness is false due
to unresolved WHO GHO licence scope, unresolved WPS workbook licence/commercial scope, and ICP's
warning against precise strict rankings.

`2026-07-18.1` was an immutable intermediate stabilization release. Its country-set sensitivity test
trimmed list positions instead of the actual minimum and maximum. It remains unchanged and inactive;
`2026-07-18.2` corrects the experiment and is the active local release.

Replay:

```powershell
python -m konsider.ingestion.worker replay data\releases\2026-07-18.2
```

Verification on 2026-07-18:

- `python -m unittest discover -s tests`: 29 tests passed, including unit, integration, failure,
  publication, checksum-tamper, pagination, sensitivity, and both release replay paths.
- `python -m compileall -q src tests`: passed.
- replay of `2026-07-18.2` and the unchanged `2026-07-17.1` baseline: passed.
- `pytest` was not installed in the bundled runtime; no test failure was hidden by that tooling
  limitation because the project suite also uses standard-library `unittest` discovery.
